# This file will get included upon `find_package(fesom ... )`

@PACKAGE_INIT@

include(${CMAKE_CURRENT_LIST_DIR}/fesom-targets.cmake)

include(CMakeFindDependencyMacro)
# Add dependencies here of CMake projects fesom depends on that define targets.
#find_dependency(MPI COMPONENTS Fortran)